/** @type {import('tailwindcss').Config} */
module.exports = {
    theme: {
      // Some useful comment
      extend: {
        fontFamily: {
          'work_sans': ['Work Sans', sans-serif]
        },
      },
    },
    variants: {
      // Some useful comment
    },
    plugins: [
      // Some useful comment
    ]
  }